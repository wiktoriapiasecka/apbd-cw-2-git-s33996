# APBD Git Exercises

This repository contains exercises for Git and GitHub classes.

## Merge without fast-forward

The merge of `feature-max` into `main` was not fast-forward because after creating the `feature-max` branch, an additional independent commit was made directly on `main`. As a result, Git had to merge two diverging histories and create a merge commit.

## Rebase

This section documents the rebase exercise.